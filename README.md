To install this Local tunnel Forwarding library
**npm install -g localtunnel**

To Test
**npx localtunnel --port 6060**
 
 To Forward the port 
**npx localtunnel --port 6060 --subdomain ws2relayserver**
